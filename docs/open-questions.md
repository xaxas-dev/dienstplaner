# Offene Fragen

Bevor eine Annahme getroffen wird, hier nachsehen ob sie bereits entschieden ist.

| ID | Frage | Status | Entschieden am |
|----|-------|--------|----------------|
| OQ-001 | Welche Java-Version ist für Timefold Solver erforderlich? | **Entschieden:** Eclipse Temurin JDK 21 (LTS). JDK 8 unzureichend (`Runtime.version()` fehlt). JDK 25 technisch ≥ 17, aber gegen timefold 1.24.0b0 ungetestet — nicht empfohlen. | 2026-05-19 |
| OQ-002 | Muss JAVA_HOME in der Systemumgebung gesetzt sein? | **Entschieden:** Eclipse Temurin 21 installiert unter `C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot\`. `java` ist nach Installation nicht automatisch im System-PATH — JAVA_HOME muss manuell in den Windows-Systemumgebungsvariablen gesetzt werden (oder per Session: `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"`). Für den Produktivbetrieb empfiehlt sich Eintrag in `System > Environment Variables`. | 2026-05-19 |
| OQ-003 | Heatmap-Ansicht: Verfügbarkeit pro Arzt über ganzen Monat als eigene View? | **Offen:** Wurde in M4-001 als Out of Scope definiert. Umsetzung als separater Folge-Milestone möglich (z. B. M4-002). Anforderung: Monats-Übersicht aller Ärzte × Tage, farbcodiert nach Verfügbarkeit. | — |
| OQ-004 | Bulk-Availability-Endpoint für effiziente Mehrfach-Abfragen im DoctorAssignPopover? | **Offen:** In M4-001 (Schritt E) wurde `useQueries` mit per-Doctor-Requests verwendet (React-Query-Deduplication). Bei Performance-Problemen bei vielen Ärzten wäre ein einzelner `GET /api/plans/{id}/shifts/{date}/availability`-Endpoint (alle Ärzte für ein Datum) die saubere Lösung. Folge-Optimierung, kein Scope für M4. | — |
| OQ-005 | DatePicker-Komponente: shadcn/react-day-picker vs. HTML `<input type="date">`? | **Entschieden (M4-001, ADR-057):** HTML `<input type="date">` bleibt Standard für lokale Single-User-Desktop-App. Kein extra Paket. Konsistentes Pattern in AbsenceFormDialog, INAExclusionFormDialog, EmploymentPeriodForm. | 2026-05-21 |
| OQ-006 | Tarif-Werte-Quelle: Aus welcher Quelle lesen konkrete Tarif-Schwellen (max. Wochenstunden, Ruhezeiten, ArbZG-Grenzwerte)? Optionen: (A) Hardcodiert in Regel-Klasse (nicht änderbar ohne Code-Deploy), (B) `app_settings`-Key-Value-Tabelle (schon vorhanden, ADR-019), (C) neues `RuleOverride`-Modell mit Arzt+Regel+Zeitraum (Phase-B-Override-Mechanismus A/B/C). | **Entschieden: Option A — hardcodiert als Klassenkonstanten in `tarif_rules.py`-Regelklassen.** Werte ändern sich nur bei Tarifverhandlungen (mehrjähriger Turnus); Code-Update bei Änderungstarifvertrag akzeptabel für lokale Einzel-App. Option B braucht Admin-UI ohne Mehrwert. Konkrete Werte aus TV-Ärzte/TdL i.d.F. 9. ÄnderungsTV (2025): Regelarbeitszeit ab 01.01.2026 = 40 h/Woche; ArbZG-Standard = 48 h/Woche; Opt-out BD-Stufe I = 58 h/Woche, BD-Stufe II = 54 h/Woche (je Durchschnitt 1 Jahr); BD-Limit = 4/Monat (Standard); Wochenend-Limit = 2/Monat; Nachtarbeit = 20:00–06:00 Uhr. | 2026-05-29 |
| OQ-007 | Excel-Schema-Anpassung an klinikinternes Tool: Welches Spalten-/Sheet-Layout erwartet das klinikinterne Tool genau? M6-001 liefert ein pragmatisches Default-Schema (ein Sheet `Dienste`, eine Zeile pro Shift — ADR-064). Sobald die Spec des klinikinternen Tools vorliegt, kann das Schema additiv angepasst werden (kein Re-Plumbing der Export-Pipeline nötig). | **Offen** | — |
| OQ-008 | Soll Fairness über Plan-Grenzen aggregieren? M8-004 optimiert Fairness pro Plan-Zeitraum (Monat). Langfristige Belastung (rolling-window über mehrere Monate) wird nicht berücksichtigt. Sinnvoll wenn Ärzte häufig urlaubsbedingt wenig Dienste in einem Monat hatten und im Folgemonat ausgeglichen werden sollen. Technisch: Plan-übergreifende Snapshot-Berechnung nötig (andere Datenquelle als OQ-006). | **Offen** | — |
| OQ-009 | Sollen Soft-Constraint-Gewichte konfigurierbar sein? Aktuell alle Soft-Constraints mit `HardSoftScore.ONE_SOFT` (Gewicht 1). Bei mehreren Soft-Constraints (M8-005+) könnte relative Gewichtung (z. B. Fairness wichtiger als Schichtfolge-Präferenz) sinnvoll sein. Timefold: `penalize_configurable()` mit `ConstraintWeight`. | **Entschieden: Nein — vorerst fixe Gewichte (`HardSoftScore.ONE_SOFT`).** Relative Gewichtung zwischen Soft-Constraints ist erst relevant wenn ≥ 2 Soft-Constraints im Einsatz sind und sie sich messbar gegenseitig verdrängen. `penalize_configurable()` ist ein Refactoring ohne neuen Nutzen für Phase B. Nachrüstbar per ADR wenn konkreter Bedarf entsteht. | 2026-05-29 |
