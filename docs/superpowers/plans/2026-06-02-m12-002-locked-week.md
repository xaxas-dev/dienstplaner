# M12-002: INA-Nachtdienstwochen als Input Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `Shift.is_locked` einführen, gesperrte Nachtdienstwochen (5× N-Shift So–Do, ein Arzt) per Dialog anlegen und im Grid read-only darstellen.

**Architecture:** Neues `is_locked`-Flag auf `Shift` (ORM + Migration). Dedizierter `POST /api/plans/{id}/locked-week`-Endpoint erzeugt atomar 5 gesperrte Shifts. Frontend: `LockedWeekDialog` in PlanPage, `UnifiedShiftCell` rendert gesperrte Shifts grau + Schloss-Icon und blockiert Klick/DnD.

**Tech Stack:** Python/FastAPI/SQLAlchemy/Alembic, React 18/TypeScript/TanStack Query, shadcn/ui, Lucide Icons, Sonner Toasts, date-fns, pytest, vitest.

---

## Datei-Map

| Aktion | Pfad |
|---|---|
| Neu | `backend/alembic/versions/0013_shift_is_locked.py` |
| Modify | `backend/app/models/shift.py` |
| Modify | `backend/app/schemas/shift.py` |
| Neu | `backend/app/schemas/locked_week.py` |
| Neu | `backend/app/services/locked_week_service.py` |
| Modify | `backend/app/api/plans.py` |
| Neu | `backend/tests/services/test_locked_week_service.py` |
| Modify | `backend/tests/integration/test_plans_api.py` |
| Modify | `frontend/src/lib/api-types.ts` |
| Neu | `frontend/src/features/plans/useCreateLockedWeek.ts` |
| Modify | `frontend/src/features/plans/components/UnifiedShiftCell.tsx` |
| Neu | `frontend/src/features/plans/components/LockedWeekDialog.tsx` |
| Modify | `frontend/src/features/plans/PlanPage.tsx` |

---

## Task 1: Migration 0013 — `shifts.is_locked`

**Files:**
- Create: `backend/alembic/versions/0013_shift_is_locked.py`

- [ ] **Step 1: Migration anlegen**

Inhalt:

```python
"""add is_locked to shifts

Revision ID: 0013
Revises: 0012
Create Date: 2026-06-02
"""

from __future__ import annotations

import sqlalchemy as sa

from alembic import op

revision: str = "0013"
down_revision: str | None = "0012"
branch_labels = None
depends_on = None


def upgrade() -> None:
    with op.batch_alter_table("shifts") as batch_op:
        batch_op.add_column(
            sa.Column(
                "is_locked",
                sa.Boolean(),
                nullable=False,
                server_default=sa.false(),
            )
        )


def downgrade() -> None:
    with op.batch_alter_table("shifts") as batch_op:
        batch_op.drop_column("is_locked")
```

- [ ] **Step 2: Migration anwenden und prüfen**

```bash
cd backend
uv run alembic upgrade head
uv run alembic current
```

Erwartete Ausgabe: `0013 (head)`

- [ ] **Step 3: Commit**

```bash
git add backend/alembic/versions/0013_shift_is_locked.py
git commit -m "feat(db): Migration 0013 — shifts.is_locked (M12-002)"
```

---

## Task 2: ORM + ShiftResponse aktualisieren

**Files:**
- Modify: `backend/app/models/shift.py`
- Modify: `backend/app/schemas/shift.py`
- Test: `backend/tests/unit/test_shift_schema.py`

- [ ] **Step 1: Failing test schreiben**

Datei `backend/tests/unit/test_shift_schema.py` — bestehenden Test ergänzen oder neue Funktion hinzufügen:

```python
def test_shift_response_has_is_locked():
    """ShiftResponse muss is_locked exponieren."""
    from app.schemas.shift import ShiftResponse
    fields = ShiftResponse.model_fields
    assert "is_locked" in fields
    assert fields["is_locked"].default is False
```

- [ ] **Step 2: Test ausführen — erwarteter Fehler**

```bash
cd backend
uv run pytest tests/unit/test_shift_schema.py::test_shift_response_has_is_locked -v
```

Erwartete Ausgabe: `FAILED — AssertionError`

- [ ] **Step 3: ORM aktualisieren**

In `backend/app/models/shift.py` nach `is_pinned` einfügen:

```python
is_locked: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)
```

- [ ] **Step 4: Schema aktualisieren**

In `backend/app/schemas/shift.py` in der Klasse `ShiftResponse` nach `updated_at` einfügen:

```python
is_locked: bool = False
```

Die vollständige `ShiftResponse`-Klasse sieht dann so aus:

```python
class ShiftResponse(ShiftBase):
    id: int
    created_at: datetime
    updated_at: datetime
    is_locked: bool = False

    model_config = ConfigDict(from_attributes=True)
```

`ShiftCreate` und `ShiftUpdate` bleiben unverändert — `is_locked` wird nicht über normale Shift-Endpoints gesetzt.

- [ ] **Step 5: Test ausführen — erwartet: PASS**

```bash
cd backend
uv run pytest tests/unit/test_shift_schema.py -v
```

Erwartete Ausgabe: alle Tests `PASSED`

- [ ] **Step 6: Gesamten Backend-Test-Smoke**

```bash
cd backend
uv run pytest --tb=short -q
```

Erwartete Ausgabe: alle Tests grün (keine Regression)

- [ ] **Step 7: Commit**

```bash
git add backend/app/models/shift.py backend/app/schemas/shift.py backend/tests/unit/test_shift_schema.py
git commit -m "feat(backend): Shift.is_locked ORM-Feld und ShiftResponse (M12-002)"
```

---

## Task 3: LockedWeek-Schemas und Service

**Files:**
- Create: `backend/app/schemas/locked_week.py`
- Create: `backend/app/services/locked_week_service.py`
- Create: `backend/tests/services/test_locked_week_service.py`

- [ ] **Step 1: Failing tests schreiben**

Neue Datei `backend/tests/services/test_locked_week_service.py`:

```python
"""Tests für locked_week_service."""
from datetime import date

import pytest
from sqlalchemy.orm import Session

from app.models.department import Department
from app.models.doctor import Doctor
from app.models.plan import Plan
from app.models.shift_type import ShiftType
from app.schemas.locked_week import LockedWeekCreate
from app.services import locked_week_service
from app.services.exceptions import PlanNotFoundError


# ---------------------------------------------------------------------------
# Fixtures
# ---------------------------------------------------------------------------

@pytest.fixture
def department(db: Session) -> Department:
    dept = Department(name="INA", display_order=1)
    db.add(dept)
    db.flush()
    return dept


@pytest.fixture
def plan(db: Session) -> Plan:
    p = Plan(
        name="Juni 2026",
        valid_from=date(2026, 6, 1),
        valid_to=date(2026, 6, 30),
        status="draft",
    )
    db.add(p)
    db.flush()
    return p


@pytest.fixture
def doctor(db: Session) -> Doctor:
    d = Doctor(
        first_name="Anna",
        last_name="Müller",
        short_name="AMü",
        email="a.mueller@klinik.de",
        is_active=True,
    )
    db.add(d)
    db.flush()
    return d


@pytest.fixture
def shift_type_n(db: Session) -> ShiftType:
    st = ShiftType(
        name="Nachtdienst",
        short_name="N",
        display_order=1,
        applies_on_weekend=True,
    )
    db.add(st)
    db.flush()
    return st


# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------

SUNDAY_2026_06_07 = date(2026, 6, 7)   # Sonntag
MONDAY_2026_06_08 = date(2026, 6, 8)   # Montag


def test_create_locked_week_creates_5_shifts(db, plan, doctor, shift_type_n):
    """5 aufeinanderfolgende Shifts So–Do werden erstellt."""
    data = LockedWeekCreate(
        doctor_id=doctor.id,
        start_date=SUNDAY_2026_06_07,
        shift_type_id=shift_type_n.id,
    )
    result = locked_week_service.create_locked_week(db, plan.id, data)

    assert len(result.created) == 5
    assert len(result.skipped) == 0
    dates = [s.shift_date for s in result.created]
    assert dates == [date(2026, 6, 7 + i) for i in range(5)]
    for s in result.created:
        assert s.is_locked is True
        assert s.is_pinned is True
        assert s.doctor_id == doctor.id


def test_create_locked_week_raises_on_non_sunday(db, plan, doctor, shift_type_n):
    """Kein Sonntag → ValueError."""
    data = LockedWeekCreate(
        doctor_id=doctor.id,
        start_date=MONDAY_2026_06_08,
        shift_type_id=shift_type_n.id,
    )
    with pytest.raises(ValueError, match="Sonntag"):
        locked_week_service.create_locked_week(db, plan.id, data)


def test_create_locked_week_raises_on_unknown_plan(db, doctor, shift_type_n):
    """Unbekannte plan_id → PlanNotFoundError."""
    data = LockedWeekCreate(
        doctor_id=doctor.id,
        start_date=SUNDAY_2026_06_07,
        shift_type_id=shift_type_n.id,
    )
    with pytest.raises(PlanNotFoundError):
        locked_week_service.create_locked_week(db, 99999, data)


def test_create_locked_week_skips_existing_shifts(db, plan, doctor, shift_type_n):
    """Bereits existierende Shifts werden übersprungen, nicht dupliziert."""
    from app.models.shift import Shift

    # Shift für Sonntag vorab anlegen
    existing = Shift(
        plan_id=plan.id,
        shift_date=SUNDAY_2026_06_07,
        shift_type_id=shift_type_n.id,
        doctor_id=doctor.id,
    )
    db.add(existing)
    db.flush()

    data = LockedWeekCreate(
        doctor_id=doctor.id,
        start_date=SUNDAY_2026_06_07,
        shift_type_id=shift_type_n.id,
    )
    result = locked_week_service.create_locked_week(db, plan.id, data)

    assert len(result.created) == 4  # Mo–Do neu
    assert len(result.skipped) == 1
    assert existing.id in result.skipped
```

- [ ] **Step 2: Tests ausführen — erwarteter Fehler**

```bash
cd backend
uv run pytest tests/services/test_locked_week_service.py -v
```

Erwartete Ausgabe: `ERROR — ModuleNotFoundError: No module named 'app.schemas.locked_week'`

- [ ] **Step 3: Schema-Datei anlegen**

Neue Datei `backend/app/schemas/locked_week.py`:

```python
from __future__ import annotations

from datetime import date

from pydantic import BaseModel

from app.schemas.shift import ShiftResponse


class LockedWeekCreate(BaseModel):
    doctor_id: int
    start_date: date
    shift_type_id: int


class LockedWeekResult(BaseModel):
    created: list[ShiftResponse]
    skipped: list[int]
```

- [ ] **Step 4: Service anlegen**

Neue Datei `backend/app/services/locked_week_service.py`:

```python
from datetime import timedelta
from sqlalchemy.orm import Session

from app.models.shift import Shift
from app.repositories import plan_repository
from app.schemas.locked_week import LockedWeekCreate, LockedWeekResult
from app.schemas.shift import ShiftResponse
from app.services.exceptions import PlanNotFoundError


def create_locked_week(db: Session, plan_id: int, data: LockedWeekCreate) -> LockedWeekResult:
    plan = plan_repository.get_plan(db, plan_id)
    if plan is None:
        raise PlanNotFoundError(plan_id)

    if data.start_date.weekday() != 6:
        raise ValueError("start_date muss ein Sonntag sein")

    created: list[ShiftResponse] = []
    skipped: list[int] = []

    for i in range(5):  # Sonntag bis Donnerstag
        shift_date = data.start_date + timedelta(days=i)
        existing = (
            db.query(Shift)
            .filter(
                Shift.plan_id == plan_id,
                Shift.shift_date == shift_date,
                Shift.shift_type_id == data.shift_type_id,
            )
            .first()
        )
        if existing is not None:
            skipped.append(existing.id)
        else:
            shift = Shift(
                plan_id=plan_id,
                shift_date=shift_date,
                shift_type_id=data.shift_type_id,
                doctor_id=data.doctor_id,
                is_locked=True,
                is_pinned=True,
            )
            db.add(shift)
            db.flush()
            created.append(ShiftResponse.model_validate(shift))

    db.commit()
    return LockedWeekResult(created=created, skipped=skipped)
```

- [ ] **Step 5: Tests ausführen — erwartet: PASS**

```bash
cd backend
uv run pytest tests/services/test_locked_week_service.py -v
```

Erwartete Ausgabe: 4 Tests `PASSED`

- [ ] **Step 6: Ruff**

```bash
cd backend
uv run ruff check app/schemas/locked_week.py app/services/locked_week_service.py --fix
uv run ruff format app/schemas/locked_week.py app/services/locked_week_service.py
```

- [ ] **Step 7: Commit**

```bash
git add backend/app/schemas/locked_week.py backend/app/services/locked_week_service.py backend/tests/services/test_locked_week_service.py
git commit -m "feat(backend): LockedWeekService + Schemas (M12-002)"
```

---

## Task 4: API-Endpoint `POST /api/plans/{plan_id}/locked-week`

**Files:**
- Modify: `backend/app/api/plans.py`
- Modify: `backend/tests/integration/test_plans_api.py`

- [ ] **Step 1: Failing Integrations-Tests schreiben**

In `backend/tests/integration/test_plans_api.py` am Ende ergänzen:

```python
# ---------------------------------------------------------------------------
# POST /api/plans/{plan_id}/locked-week
# ---------------------------------------------------------------------------

def test_create_locked_week_returns_201(client, db_session):
    """Gültiger Request → 201 + LockedWeekResult."""
    from datetime import date as dt
    # Plan, Doctor, ShiftType anlegen
    from app.models.plan import Plan
    from app.models.doctor import Doctor
    from app.models.shift_type import ShiftType

    plan = Plan(name="Test", valid_from=dt(2026, 6, 1), valid_to=dt(2026, 6, 30), status="draft")
    doctor = Doctor(first_name="A", last_name="B", short_name="AB", email="a@b.de", is_active=True)
    stype = ShiftType(name="Nacht", short_name="N", display_order=1, applies_on_weekend=True)
    db_session.add_all([plan, doctor, stype])
    db_session.commit()

    resp = client.post(
        f"/api/plans/{plan.id}/locked-week",
        json={
            "doctor_id": doctor.id,
            "start_date": "2026-06-07",   # Sonntag
            "shift_type_id": stype.id,
        },
    )
    assert resp.status_code == 201
    body = resp.json()
    assert len(body["created"]) == 5
    assert body["skipped"] == []
    # Alle created Shifts haben is_locked=True
    for s in body["created"]:
        assert s["is_locked"] is True
        assert s["is_pinned"] is True


def test_create_locked_week_422_on_non_sunday(client, db_session):
    """Kein Sonntag → 422."""
    from datetime import date as dt
    from app.models.plan import Plan
    from app.models.doctor import Doctor
    from app.models.shift_type import ShiftType

    plan = Plan(name="Test2", valid_from=dt(2026, 6, 1), valid_to=dt(2026, 6, 30), status="draft")
    doctor = Doctor(first_name="C", last_name="D", short_name="CD", email="c@d.de", is_active=True)
    stype = ShiftType(name="Nacht2", short_name="N2", display_order=2, applies_on_weekend=True)
    db_session.add_all([plan, doctor, stype])
    db_session.commit()

    resp = client.post(
        f"/api/plans/{plan.id}/locked-week",
        json={
            "doctor_id": doctor.id,
            "start_date": "2026-06-08",   # Montag
            "shift_type_id": stype.id,
        },
    )
    assert resp.status_code == 422


def test_create_locked_week_404_on_unknown_plan(client):
    """Unbekannte plan_id → 404."""
    resp = client.post(
        "/api/plans/99999/locked-week",
        json={"doctor_id": 1, "start_date": "2026-06-07", "shift_type_id": 1},
    )
    assert resp.status_code == 404
```

- [ ] **Step 2: Tests ausführen — erwarteter Fehler**

```bash
cd backend
uv run pytest tests/integration/test_plans_api.py::test_create_locked_week_returns_201 -v
```

Erwartete Ausgabe: `FAILED — 404 Not Found` (Endpoint existiert noch nicht)

- [ ] **Step 3: Endpoint in `plans.py` eintragen**

In `backend/app/api/plans.py` Imports ergänzen:

```python
from app.schemas.locked_week import LockedWeekCreate, LockedWeekResult
from app.services import locked_week_service
```

Dann neuen Router-Endpoint ergänzen (nach dem `/export`-Endpoint):

```python
@router.post(
    "/{plan_id}/locked-week",
    response_model=LockedWeekResult,
    status_code=status.HTTP_201_CREATED,
)
def create_locked_week(
    plan_id: int, data: LockedWeekCreate, db: Session = Depends(get_db)
) -> LockedWeekResult:
    try:
        return locked_week_service.create_locked_week(db, plan_id, data)
    except PlanNotFoundError:
        raise HTTPException(status_code=404, detail=f"Plan {plan_id} nicht gefunden")
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc))
```

- [ ] **Step 4: Tests ausführen — erwartet: PASS**

```bash
cd backend
uv run pytest tests/integration/test_plans_api.py -k "locked_week" -v
```

Erwartete Ausgabe: 3 Tests `PASSED`

- [ ] **Step 5: Vollständiger Backend-Test-Smoke**

```bash
cd backend
uv run pytest --tb=short -q
```

Erwartete Ausgabe: alle Tests grün

- [ ] **Step 6: Ruff**

```bash
cd backend
uv run ruff check app/api/plans.py --fix
uv run ruff format app/api/plans.py
```

- [ ] **Step 7: Commit**

```bash
git add backend/app/api/plans.py backend/tests/integration/test_plans_api.py
git commit -m "feat(backend): POST /api/plans/{id}/locked-week Endpoint (M12-002)"
```

---

## Task 5: Frontend-Typen + `useCreateLockedWeek`-Hook

**Files:**
- Modify: `frontend/src/lib/api-types.ts`
- Create: `frontend/src/features/plans/useCreateLockedWeek.ts`

- [ ] **Step 1: `api-types.ts` aktualisieren**

In `frontend/src/lib/api-types.ts` — in der Inline-Definition des Shift-Typs (GET /api/plans/{id}/shifts Response, `is_pinned: boolean` suchen) `is_locked: boolean` direkt darunter ergänzen:

```typescript
is_pinned: boolean;
is_locked: boolean;
```

Dasselbe für alle Stellen wo `is_pinned: boolean` im Shift-Kontext vorkommt (Suche mit grep nach `is_pinned`; alle Shift-Interfaces betroffen). Es sind typischerweise 2-3 Stellen (ShiftRead, ShiftWithDetails, ShiftUpdate-Response).

Außerdem am Ende der Datei (oder bei den anderen Request-Body-Typen) ergänzen:

```typescript
interface LockedWeekCreate {
  doctor_id: number;
  start_date: string;    // ISO 8601 (YYYY-MM-DD)
  shift_type_id: number;
}

interface LockedWeekResult {
  created: ShiftRead[];  // oder der entsprechende Inline-Shift-Typ
  skipped: number[];
}
```

Hinweis: `ShiftRead` entspricht dem Inline-Shift-Typ aus der API. Falls api-types.ts keinen benannten `ShiftRead`-Typ hat, stattdessen den Inline-Typ kopieren oder `any` temporär verwenden und TODO-Kommentar hinterlassen — niemals `ts-ignore` ohne Erklärung.

- [ ] **Step 2: TypeScript-Kompilierung prüfen**

```bash
cd frontend
pnpm tsc --noEmit
```

Erwartete Ausgabe: keine Fehler

- [ ] **Step 3: `useCreateLockedWeek.ts` anlegen**

Neue Datei `frontend/src/features/plans/useCreateLockedWeek.ts`:

```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { apiPost } from '@/lib/api'
import { shiftQueryKeys } from './usePlanShifts'

interface LockedWeekCreate {
  doctor_id: number
  start_date: string
  shift_type_id: number
}

interface LockedWeekResult {
  created: unknown[]
  skipped: number[]
}

export function useCreateLockedWeek(planId: number) {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (data: LockedWeekCreate) =>
      apiPost<LockedWeekResult>(`/api/plans/${planId}/locked-week`, data),
    onSuccess: () => {
      void qc.invalidateQueries({ queryKey: shiftQueryKeys.byPlan(planId) })
    },
  })
}
```

- [ ] **Step 4: TypeScript-Check**

```bash
cd frontend
pnpm tsc --noEmit
```

Erwartete Ausgabe: keine Fehler

- [ ] **Step 5: Commit**

```bash
git add frontend/src/lib/api-types.ts frontend/src/features/plans/useCreateLockedWeek.ts
git commit -m "feat(frontend): is_locked in api-types + useCreateLockedWeek Hook (M12-002)"
```

---

## Task 6: `UnifiedShiftCell` — Locked-Rendering und Klick-Block

**Files:**
- Modify: `frontend/src/features/plans/components/UnifiedShiftCell.tsx`
- Test: (neue Datei) `frontend/src/features/plans/tests/UnifiedShiftCell.locked.test.tsx`

- [ ] **Step 1: Failing tests schreiben**

Neue Datei `frontend/src/features/plans/tests/UnifiedShiftCell.locked.test.tsx`:

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { UnifiedShiftCell } from '../components/UnifiedShiftCell'

// dnd-kit braucht DndContext — minimaler Wrapper
import { DndContext } from '@dnd-kit/core'

const baseProps = {
  rotationId: 1,
  dayKey: '2026-06-07',
  department: { id: 1, name: 'INA', display_order: 1, color: null, is_ina: true, full_time_slots: 1 },
  inRotation: true,
  text: 'AMü',
  isWeekend: false,
  isToday: false,
  focusMode: 'alle' as const,
  shiftId: 42,
  shiftAssigned: true,
}

vi.mock('@/features/command-palette/useCommandPalette', () => ({
  useCommandPalette: () => ({ open: vi.fn(), close: vi.fn(), toggle: vi.fn(), isOpen: false }),
}))

function wrap(ui: React.ReactElement) {
  return render(<DndContext>{ui}</DndContext>)
}

describe('UnifiedShiftCell locked', () => {
  it('rendert Schloss-Icon wenn isLocked=true', () => {
    wrap(<UnifiedShiftCell {...baseProps} isLocked={true} />)
    expect(screen.getByTestId('lock-icon')).toBeInTheDocument()
  })

  it('öffnet kein Popover bei Klick wenn isLocked=true', () => {
    const onClick = vi.fn()
    wrap(<UnifiedShiftCell {...baseProps} isLocked={true} onClick={onClick} />)
    fireEvent.click(screen.getByRole('button', { hidden: true }))
    expect(onClick).not.toHaveBeenCalled()
  })

  it('rendert kein Schloss-Icon wenn isLocked=false', () => {
    wrap(<UnifiedShiftCell {...baseProps} isLocked={false} />)
    expect(screen.queryByTestId('lock-icon')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Tests ausführen — erwarteter Fehler**

```bash
cd frontend
pnpm test -- UnifiedShiftCell.locked --run
```

Erwartete Ausgabe: `FAIL — isLocked is not a prop / lock-icon not found`

- [ ] **Step 3: `UnifiedShiftCell.tsx` — Prop und Rendering ergänzen**

In `frontend/src/features/plans/components/UnifiedShiftCell.tsx`:

**3a. Import ergänzen** (oben nach bestehenden Lucide-Imports):
```tsx
import { Lock } from 'lucide-react'
```

**3b. Prop-Interface ergänzen** — in `UnifiedShiftCellProps` einfügen:
```tsx
isLocked?: boolean
```

**3c. Prop destrukturieren** — in der Funktionssignatur nach `isPinned` ergänzen:
```tsx
isLocked,
```

**3d. Locked-Rendering einbauen** — In `handleClick` ganz oben einfügen:
```tsx
function handleClick(e: React.MouseEvent) {
  if (isLocked) return   // ← NEU: gesperrte Shifts sind nicht klickbar
  const { shiftKey } = e
  // ... restlicher Code unverändert
```

**3e. Locked-Darstellung einbauen** — In der return-JSX, ganz am Anfang des äußersten `<div>` (noch vor den bestehenden Rendering-Zweigen), den locked-Zweig hinzufügen. Suche den Block wo `filled`/`idle`/etc. entschieden wird und ergänze davor:

```tsx
{/* Locked-Shift: grauer Block, Schloss-Icon, kein Klick */}
{isLocked && text && (
  <div className="absolute inset-0 flex items-center justify-center bg-zinc-200 rounded-sm">
    <Lock
      data-testid="lock-icon"
      className="absolute top-0.5 left-0.5 size-2.5 text-zinc-400"
    />
    <span className="text-zinc-500 text-[11px] font-medium leading-none">{text}</span>
  </div>
)}
```

Hinweis zur Integration: `UnifiedShiftCell` hat eine komplexe Rendering-Logik mit mehreren Zweigen (filled, idle, absence, etc.). Der locked-Zweig muss **frühzeitig** rendern und die restlichen Zweige nicht erreichen. Prüfe nach dem Einbauen, dass kein Zustand-Overlay (Conflict-Dot, Tarif-Dot) auf einem locked-Shift erscheint.

- [ ] **Step 4: Tests ausführen — erwartet: PASS**

```bash
cd frontend
pnpm test -- UnifiedShiftCell.locked --run
```

Erwartete Ausgabe: 3 Tests `PASSED`

- [ ] **Step 5: Alle Frontend-Tests**

```bash
cd frontend
pnpm test --run
```

Erwartete Ausgabe: alle Tests grün (keine Regression in anderen Komponenten)

- [ ] **Step 6: Commit**

```bash
git add frontend/src/features/plans/components/UnifiedShiftCell.tsx \
        frontend/src/features/plans/tests/UnifiedShiftCell.locked.test.tsx
git commit -m "feat(frontend): UnifiedShiftCell locked-Rendering + Klick-Block (M12-002)"
```

---

## Task 7: `LockedWeekDialog` + CommandBar-Trigger in PlanPage

**Files:**
- Create: `frontend/src/features/plans/components/LockedWeekDialog.tsx`
- Modify: `frontend/src/features/plans/PlanPage.tsx`

- [ ] **Step 1: `LockedWeekDialog.tsx` anlegen**

Neue Datei `frontend/src/features/plans/components/LockedWeekDialog.tsx`:

```tsx
import * as React from 'react'
import { useState } from 'react'
import { toast } from 'sonner'
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { useCreateLockedWeek } from '../useCreateLockedWeek'
import type { Doctor } from '@/lib/types'
import type { ShiftTypeResponse } from '@/lib/types'

interface LockedWeekDialogProps {
  open: boolean
  onClose: () => void
  planId: number
  doctors: Doctor[]
  shiftTypes: ShiftTypeResponse[]
}

export function LockedWeekDialog({ open, onClose, planId, doctors, shiftTypes }: LockedWeekDialogProps) {
  const [doctorId, setDoctorId] = useState<string>('')
  const [startDate, setStartDate] = useState<string>('')
  const [shiftTypeId, setShiftTypeId] = useState<string>('')

  const mutation = useCreateLockedWeek(planId)

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault()

    if (!doctorId || !startDate || !shiftTypeId) {
      toast.error('Alle Felder sind Pflichtfelder.')
      return
    }

    // Sonntags-Prüfung client-seitig (Backend prüft ebenfalls)
    const parsed = new Date(startDate)
    if (parsed.getDay() !== 0) {
      toast.error('Startdatum muss ein Sonntag sein.')
      return
    }

    mutation.mutate(
      {
        doctor_id: parseInt(doctorId),
        start_date: startDate,
        shift_type_id: parseInt(shiftTypeId),
      },
      {
        onSuccess: (result) => {
          if (result.skipped.length > 0) {
            toast.success(
              `Nachtdienstwoche eingetragen. ${result.skipped.length} Schicht(en) bereits vorhanden, übersprungen.`
            )
          } else {
            toast.success('Nachtdienstwoche eingetragen.')
          }
          onClose()
          setDoctorId('')
          setStartDate('')
          setShiftTypeId('')
        },
        onError: () => {
          toast.error('Fehler beim Eintragen der Nachtdienstwoche.')
        },
      }
    )
  }

  return (
    <Dialog open={open} onOpenChange={(v) => { if (!v) onClose() }}>
      <DialogContent className="sm:max-w-md">
        <DialogHeader>
          <DialogTitle>Nachtdienstwoche eintragen</DialogTitle>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="space-y-4 pt-2">
          <div className="space-y-1.5">
            <Label htmlFor="lw-doctor">Arzt</Label>
            <Select value={doctorId} onValueChange={setDoctorId}>
              <SelectTrigger id="lw-doctor">
                <SelectValue placeholder="Arzt wählen…" />
              </SelectTrigger>
              <SelectContent>
                {doctors
                  .filter((d) => d.is_active)
                  .map((d) => (
                    <SelectItem key={d.id} value={String(d.id)}>
                      {d.last_name}, {d.first_name} ({d.short_name})
                    </SelectItem>
                  ))}
              </SelectContent>
            </Select>
          </div>

          <div className="space-y-1.5">
            <Label htmlFor="lw-date">Startdatum (Sonntag)</Label>
            <input
              id="lw-date"
              type="date"
              value={startDate}
              onChange={(e) => setStartDate(e.target.value)}
              className="w-full h-9 rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm"
            />
            <p className="text-xs text-muted-foreground">Muss ein Sonntag sein (So–Do wird erzeugt).</p>
          </div>

          <div className="space-y-1.5">
            <Label htmlFor="lw-shifttype">Schichttyp</Label>
            <Select value={shiftTypeId} onValueChange={setShiftTypeId}>
              <SelectTrigger id="lw-shifttype">
                <SelectValue placeholder="Schichttyp wählen…" />
              </SelectTrigger>
              <SelectContent>
                {shiftTypes.map((st) => (
                  <SelectItem key={st.id} value={String(st.id)}>
                    {st.short_name} — {st.name}
                  </SelectItem>
                ))}
              </SelectContent>
            </Select>
          </div>

          <DialogFooter className="pt-2">
            <Button type="button" variant="outline" onClick={onClose}>
              Abbrechen
            </Button>
            <Button type="submit" variant="accent" disabled={mutation.isPending}>
              {mutation.isPending ? 'Wird eingetragen…' : 'Eintragen'}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 2: TypeScript-Prüfung**

```bash
cd frontend
pnpm tsc --noEmit
```

Erwartete Ausgabe: keine Fehler

- [ ] **Step 3: PlanPage.tsx — Dialog-State und Imports ergänzen**

In `frontend/src/features/plans/PlanPage.tsx`:

**3a. Import ergänzen** (nach den anderen Component-Imports):
```tsx
import { MoonStar } from 'lucide-react'
import { LockedWeekDialog } from './components/LockedWeekDialog'
```

**3b. State ergänzen** — im Funktionskörper (nach `const [solverPanelOpen, ...]` o. ä.):
```tsx
const [lockedWeekDialogOpen, setLockedWeekDialogOpen] = useState(false)
```

**3c. Dialog ins JSX einfügen** — direkt vor dem schließenden `</DndContext>` oder neben den anderen Modals (PlanSettingsModal, AlertDialog):
```tsx
<LockedWeekDialog
  open={lockedWeekDialogOpen}
  onClose={() => setLockedWeekDialogOpen(false)}
  planId={plan.id}
  doctors={doctors ?? []}
  shiftTypes={shiftTypes ?? []}
/>
```

**3d. CommandBar `extras` ergänzen** — in der CommandBar-Instanz (suche `<CommandBar`) den `extras`-Prop setzen oder erweitern:
```tsx
extras={
  <Button
    variant="outline"
    size="sm"
    onClick={() => setLockedWeekDialogOpen(true)}
    className="shrink-0"
  >
    <MoonStar className="size-4" />
    Nachtwoche
  </Button>
}
```

Falls `extras` bereits einen anderen Inhalt hat (z. B. Export-Button), in ein Fragment einwickeln:
```tsx
extras={
  <>
    {/* bestehender extras-Inhalt */}
    <Button
      variant="outline"
      size="sm"
      onClick={() => setLockedWeekDialogOpen(true)}
      className="shrink-0"
    >
      <MoonStar className="size-4" />
      Nachtwoche
    </Button>
  </>
}
```

- [ ] **Step 4: `isLocked` aus shifts-Daten an `UnifiedPlanGrid` durchreichen**

`UnifiedShiftCell` bekommt `isLocked` — das muss aus dem Shift-Objekt (`shift.is_locked`) in `UnifiedPlanGrid` oder `PlanPage` herausgezogen und übergeben werden. Prüfe in `frontend/src/features/plans/components/UnifiedPlanGrid.tsx`, wie `UnifiedShiftCell` instanziiert wird, und ergänze `isLocked={shift?.is_locked ?? false}`. Wenn `isPinned` bereits so weitergegeben wird, folge demselben Muster.

- [ ] **Step 5: TypeScript-Prüfung**

```bash
cd frontend
pnpm tsc --noEmit
```

Erwartete Ausgabe: keine Fehler

- [ ] **Step 6: Alle Frontend-Tests**

```bash
cd frontend
pnpm test --run
```

Erwartete Ausgabe: alle Tests grün

- [ ] **Step 7: Visuellen Smoke-Test durchführen**

Dev-Server starten (falls nicht läuft):
```bash
cd backend && uv run uvicorn app.main:app --reload &
cd frontend && pnpm dev
```

Prüfen:
1. PlanPage öffnen → Button „Nachtwoche" in CommandBar sichtbar
2. Button klicken → Dialog öffnet sich mit Arzt-/Datum-/Schichttyp-Feldern
3. Gültigen Sonntag (z. B. 2026-06-07) + Arzt + N-Schichttyp wählen → „Eintragen"
4. Toast „Nachtdienstwoche eingetragen." erscheint
5. Grid zeigt So–Do-Zellen grau mit Schloss-Icon
6. Klick auf gesperrte Zelle → kein Popover öffnet sich
7. Nicht-Sonntag eingeben → Toast „Startdatum muss ein Sonntag sein."

- [ ] **Step 8: Commit**

```bash
git add frontend/src/features/plans/components/LockedWeekDialog.tsx \
        frontend/src/features/plans/PlanPage.tsx \
        frontend/src/features/plans/components/UnifiedPlanGrid.tsx
git commit -m "feat(frontend): LockedWeekDialog + CommandBar-Trigger + isLocked Grid-Rendering (M12-002)"
```

---

## Task 8: Milestone-Abschluss

**Files:**
- Modify: `docs/roadmap.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: Roadmap aktualisieren**

In `docs/roadmap.md` Status von M12-002 auf abgeschlossen setzen:

```markdown
| M12-002 | INA-Nachtdienstwochen als Input (`Shift.is_locked`) | ✅ Abgeschlossen (2026-06-02) |
```

- [ ] **Step 2: CLAUDE.md aktualisieren**

Den bestehenden Abschnitt `Besetzungs-Layer-Sperre (M12-001)` ergänzen oder neuen Eintrag hinzufügen:

```markdown
- **Gesperrte Shifts (M12-002):** `Shift.is_locked: bool` (Default `false`).
  Markiert Input-Shifts (INA-Nachtdienstwochen), die die Planerin nicht editiert.
  Gesetzt via `POST /api/plans/{id}/locked-week` (5× N-Shift So–Do atomar).
  Frontend: grauer Block + Schloss-Icon, Klick/DnD deaktiviert.
  Backend-Delete nicht geblockt (weiche Validierung, Phase A). ADR-090.
  Abgrenzung: `is_pinned` = Solver-Konzept; `is_locked` = Editor-Konzept.
```

- [ ] **Step 3: ADR-090 in docs/decisions.md eintragen**

In `docs/decisions.md` neuen ADR ergänzen:

```markdown
## ADR-090: Shift.is_locked als UI-Only-Flag (weiche Validierung)

**Datum:** 2026-06-02
**Status:** Entschieden

**Kontext:** INA-Nachtdienstwochen kommen als Input aus dem Besetzungsplan.
Die Planerin soll sie nicht editieren können. Backend-Constraints würden den
Phase-A-Grundsatz (weiche Validierung) verletzen.

**Entscheidung:** `Shift.is_locked` ist ein UI-Only-Flag. Das Backend-Delete ist
nicht geblockt. Der Frontend-Klick- und DnD-Handler prüft `is_locked` und
bricht früh ab. Dasselbe Muster wie `Plan.besetzung_locked` (ADR-089).

**Konsequenz:** Direkte API-Aufrufe können locked Shifts überschreiben/löschen.
Das ist in Phase A akzeptiert.
```

- [ ] **Step 4: Vollständiger Backend-Test-Smoke**

```bash
cd backend
uv run pytest --tb=short -q
```

Erwartete Ausgabe: alle Tests grün

- [ ] **Step 5: Vollständiger Frontend-Test-Smoke**

```bash
cd frontend
pnpm test --run
```

Erwartete Ausgabe: alle Tests grün

- [ ] **Step 6: Abschluss-Commit**

```bash
git add docs/roadmap.md CLAUDE.md docs/decisions.md
git commit -m "docs: M12-002 Abschluss — ADR-090, CLAUDE.md, Roadmap"
```

---

## Self-Review

**Spec-Abgleich:**

| Spec-Anforderung | Task |
|---|---|
| `Shift.is_locked` Feld + Migration | Task 1 + 2 |
| `LockedWeekCreate` / `LockedWeekResult` Schemas | Task 3 |
| `create_locked_week` Service: atomar, So–Do, is_locked+is_pinned | Task 3 |
| `POST /api/plans/{id}/locked-week` Endpoint | Task 4 |
| Service-Test: 5 Shifts So–Do | Task 3 |
| Service-Test: Nicht-Sonntag → Fehler | Task 3 |
| Service-Test: Shift existiert → skipped | Task 3 |
| API-Test: 201, 422, 404 | Task 4 |
| `api-types.ts` manuell ergänzt | Task 5 |
| `useCreateLockedWeek` Hook | Task 5 |
| UnifiedShiftCell: locked-Rendering grau + Schloss | Task 6 |
| UnifiedShiftCell: kein Klick bei isLocked | Task 6 |
| Frontend-Tests locked-Rendering | Task 6 |
| LockedWeekDialog: Arzt + Startdatum + ShiftType | Task 7 |
| CommandBar-Button „Nachtwoche" | Task 7 |
| Toast bei skipped > 0 | Task 7 |
| Visueller Smoke-Test | Task 7 |
| Milestone-Doku + ADR-090 | Task 8 |

**Typ-Konsistenz:** `LockedWeekCreate` (Backend-Schema, Hook-Interface), `LockedWeekResult` (Backend-Schema, Hook-Return-Type) — identisch in Tasks 3, 5, 7. `ShiftResponse.is_locked` eingeführt in Task 2, konsumiert in Tasks 4, 6.

**Keine Platzhalter:** Alle Code-Blöcke vollständig. Alle Pfade absolut. Alle Kommandos mit erwarteter Ausgabe.
