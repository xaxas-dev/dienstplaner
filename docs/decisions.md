# Architektur-Entscheidungen

| ID | Entscheidung | Begründung | Datum |
|----|-------------|------------|-------|
| ADR-001 | SQLite als Datenbank | Single-User, lokal, kein Server-Overhead | 2026-05-05 |
| ADR-002 | uv als Python-Paketmanager | Schnell, reproduzierbar, lockfile-basiert | 2026-05-05 |
| ADR-003 | pnpm als Node-Paketmanager | Effizient, strikte Abhängigkeitsauflösung | 2026-05-05 |
| ADR-004 | weiterbildungsjahr ohne oberes Limit | Weiterbildungen am UKSH dauern real bis ca. 10 Jahre; das ursprünglich angenommene Maximum von 6 ist falsch. Validierung: nur >= 1 in Pydantic, kein DB-Constraint. | 2026-05-07 |
| ADR-005 | virtual_entry_date manuell gepflegt (vorerst) | Das virtuelle Eintrittsdatum ergibt sich aus Eintrittsdatum plus Anrechnungszeiten. Berechnung wird in einem späteren Meilenstein automatisiert. Erste Version: manuelles Feld, nullable. | 2026-05-07 |
| ADR-006 | T1 als globaler Schichttyp (nicht bereichsgebunden im Schema) | T1 (Tagdienst INA) ist fachlich nur für die Interdisziplinäre Notaufnahme relevant, wird aber als globaler ShiftType modelliert. Die Eingrenzung auf den Bereich INA erfolgt als Solver-Constraint in M8, nicht als Schema-Beziehung. | 2026-05-07 |
| ADR-007 | Bestehender "Tagdienst" (T) bleibt unverändert; T1 kommt als neue Entität dazu | Variante a gewählt: "Tagdienst" (short_name=T) ist im Klinikalltag als eigenständiger Dienst bekannt und bleibt unverändert. T1 ist ein separater Schichttyp. Umbenennung des bestehenden zu T2 wurde nicht vorgenommen (unnötige Datenmigration). | 2026-05-07 |
| ADR-008 | Sollbesetzung mit min/max statt Zielwert | Eine einzelne Zielzahl würde die Planungsrealität verfälschen. min/max bildet den akzeptablen Bereich ab: z.B. SU 6–8 bedeutet, dass 6, 7 oder 8 Stellen alle gültig sind. max=null bedeutet keine Obergrenze; max=0 bedeutet "nicht besetzbar". Werte aus Excel-Stichprobe als Seed-Defaults, manuell anpassbar ohne Überschreiben durch erneutes Seeden. | 2026-05-07 |
| ADR-009 | weiterbildungsjahr als computed property statt gespeichertes Feld | Gespeicherte Werte veralten bei Beschäftigungsunterbrechungen, Elternzeit oder schlicht wenn kein Adminzugriff erfolgt. Da entry_date vorhanden ist, kann WBJ immer korrekt berechnet werden: `floor((heute - entry_date).days / 365.25) + 1`. Fachärzte und Ärzte ohne entry_date erhalten null. Das Feld erscheint weiterhin in der API (Pydantic v2 @computed_field), ist für den Frontend-Typen transparent. | 2026-05-07 |
| ADR-010 | Zwei neue externe Bereiche Intensiv (NCH) und Intensiv extern | In den ersten Excel-Auswertungen nicht vollständig erfasst. Beide sind externe Rotationen (is_external=True, is_shift_relevant=False). Sollbesetzung jeweils 1–1. | 2026-05-07 |
| ADR-011 | Hybrid-Modell: Schicht ohne direkten Bereich | Eine Shift speichert nur (Datum, Schichttyp, Arzt). Der Bereich ergibt sich aus der aktiven RotationAssignment des Arztes zur Schicht-Zeit. Vorteil: Schichten müssen bei Rotationsänderungen nicht migriert werden; unbesetzte Schichten haben naturgemäß keinen Bereich. Nachteil: Bereichsermittlung erfordert Join-Logik im Service. | 2026-05-07 |
| ADR-012 | Plan-Versionierung als JSON-Snapshot statt Schatten-Tabellen | Vollständige Snapshots des Planstands werden als JSON-Text in plan_versions.snapshot_json abgelegt. Alternative wären Schatten-Tabellen (shifts_history etc.), die aber erheblichen Schema-Overhead erzeugen. JSON-Snapshots sind einfacher, für SQLite ausreichend, und erlauben Restore ohne Schemadopplung. Kein JSON-Pfad-Querying in SQLite nötig. | 2026-05-07 |
| ADR-013 | Plan immer editierbar (kein Lock nach RELEASED) | Status RELEASED signalisiert "an Ärzte versendet", sperrt aber die Datenbank nicht. Krankheitsausfälle und kurzfristige Änderungen müssen auch nach Release möglich bleiben. ARCHIVED ist nur ein UI-Hinweis. Kein DB-Constraint verhindert Writes. | 2026-05-07 |
| ADR-014 | Wünsche zunächst nur date-basiert | Recurring-Wünsche (z.B. "jeden Mittwoch frei") und Cross-Day-Constraints (z.B. "kein T-Dienst nach N-Dienst") erhöhen die Schemakomplexität erheblich. Für die erste Version reicht date-basiertes wish_date. Erweiterung folgt in einem späteren Meilenstein. | 2026-05-07 |
| ADR-015 | Geteilte Rotationen ohne UNIQUE-Constraint | Mehrere RotationAssignment-Einträge mit überlappenden Zeiträumen für denselben (plan_id, doctor_id, department_id) sind explizit erlaubt. Zwei Teilzeit-Ärzte können sich eine Rotation teilen – das Modell speichert beide Einträge separat. Ein UNIQUE-Constraint würde diesen Anwendungsfall verhindern. Fachliche Validierung (z.B. 100%-Äquivalenz) obliegt dem Solver. | 2026-05-07 |
| ADR-016 | INA-Verfügbarkeit aus drei Quellen, Service-Funktion zentral | `get_ina_availability` prüft Rotation, INAExclusion und Absence in einer Funktion. Kein Constraint-Splitting über mehrere Stellen. Die Anwendung im Plan-Editor (M3) und Solver (M8) importiert diese eine Funktion. | 2026-05-09 |
| ADR-017 | `blocks_ina_weekdays`/`blocks_ina_weekends` getrennt | Werktag und Wochenende werden unabhängig gesteuert, weil CK nur werktags blockiert, am Wochenende aber verfügbar ist. Ein einzelnes `blocks_ina`-Flag würde diesen Sonderfall nicht abbilden. | 2026-05-09 |
| ADR-018 | Schwangerschaft als INAExclusion, kein eigener Doctor-Status | Ein separater `is_pregnant`-Status am Arzt wäre statisch. INAExclusion ist zeitabhängig (valid_from/valid_to) und kann neben anderen Gründen (Einarbeitung, Sonstiges) parallel existieren. Mutterschutz und Elternzeit sind bereits als AbsenceType modelliert und blockieren über den Absence-Pfad. | 2026-05-09 |
| ADR-019 | Key-Value-Settings statt typisierte Konfigurations-Tabelle | Flexible, erweiterbar ohne Migrationen. Weitere Settings können über Migration ergänzt werden ohne Code-Änderung im Backend oder Frontend. Typisierung (Integer, Boolean) wird bei Bedarf clientseitig ergänzt. | 2026-05-12 |
| ADR-020 | clinic_name als initiale Einstellung, weitere Settings über Migration | Nur clinic_name wird initial benötigt. Das Schema unterstützt beliebig viele weitere Einstellungen, die per Migration ohne API- oder Frontend-Änderung sichtbar werden (SettingsPage iteriert über alle Settings). | 2026-05-12 |
| ADR-021 | dev.ps1 führt alembic upgrade head automatisch aus | Vermeidet manuelle Migrations-Schritte im Entwicklungs-Workflow. Bei Fehler bricht das Skript ab und startet das Backend nicht, um inkonsistente DB-Zustände zu verhindern. | 2026-05-12 |
| ADR-022 | Design-System nach Atelier-Look übernommen (warm, papierfarben) | Hintergrund `#F6F1E6`, Schrift Geist (UI) und Newsreader (Headings/KPIs), Terracotta-Akzent. Designziel: lesbar, ruhig, professionell – kein generisch-weißes SaaS-Layout. Quelle: `design-reference/Dienstplaner_Redesign.html`. | 2026-05-12 |
| ADR-023 | Token-basiertes Design-System, keine Hex-Codes außerhalb von tokens.ts/shift-palette.ts | Alle Farben, Radien und Schriften stehen in `src/lib/design/tokens.ts` (Tailwind und JS-Verwendung) und `src/lib/design/shift-palette.ts` (Schichtfarben). Tailwind-Klassen wie `bg-paper`, `text-ink`, `bg-warn-bg` leiten direkt aus diesen Tokens ab. Kein Magic-Number-Hex in Komponenten. | 2026-05-12 |
| ADR-024 | UI-Primitives in src/components/dp/, separat von shadcn/ui | Domänenspezifische Komponenten (`Chip`, `ShiftChip`, `ShiftCell`, `Avatar`, `KpiTile`, `Sparkline`) leben in `components/dp/`. shadcn-Komponenten in `components/ui/` bleiben unverändert. Klare Trennung: shadcn für generische Dialoge/Formulare, dp/ für Planungs-UI. | 2026-05-12 |
| ADR-025 | MiniRail (60 px) statt klassische Sidebar (240 px) | Kompaktere Navigation schafft Platz für den Plan-Grid und betont Inhalte statt Navigation. Abweichungen von Anleitung §3: (1) Settings als eigenes Rail-Item zwischen Spacer und Avatar — direkter Zugriff auf häufigsten Sekundär-Pfad ohne User-Menu; (2) clinic_name als Sub-Label unter Avatar-Tile — bleibt sichtbar ohne die CommandBar zu belasten. | 2026-05-13 |
| ADR-026 | CommandBar ist page-spezifisch, keine globale Top-Bar | Jede Seite rendert ihre eigene CommandBar mit eigenen Props (Titel, Filter, Aktionen). Kein zentraler Zustand in der Shell. Vorteil: Pages sind selbsterklärend, kein Prop-Drilling durch Shell-Layer. Konsequenz: Stammdaten-Pages erhalten CommandBar erst in M1-011 (Migration), bis dahin behalten sie ihre Page-Header. | 2026-05-13 |
| ADR-027 | KpiBar in M1-010 minimal (nur Tile-Container) | Anleitung §5 spezifiziert KpiBar mit Sparkline und Tab-Group. Da in M1-010 noch keine echten Plan-Daten vorliegen, wird nur der Tile-Container implementiert. Sparkline und Tab-Group kommen in M2-003 mit echten Daten und Sub-Views. Kein Over-Engineering für hypothetische Daten. | 2026-05-13 |
| ADR-028 | shadcn-Button erhält neue Variante `accent` (terracotta) | Primär-CTAs in CommandBar und ähnlichen Flächen sollen den Terracotta-Akzent verwenden. Da `accent` in shadcn belegt ist (shadcn-System), wird die neue Variante ebenfalls `accent` genannt, aber mit dp-Tokens belegt: `bg-dp-accent hover:bg-dp-accent-hover rounded-full`. Der Hover-Wert `#B45B30` ist als `accentHover` in `tokens.ts` registriert. | 2026-05-13 |
| ADR-029 | Ärzte-Liste als 3-spaltiges Card-Grid statt Tabelle | DoctorCard enthält mehr kontextreiche Informationen (Qualifikationen, Heatmap, Beschäftigungsgrad) als eine Tabellenzeile sinnvoll zeigen könnte. Ein Card-Grid ist scanbar und skaliert gut auf 20–50 Ärzte. Für sehr große Listen (>100) würde eine Tabelle bevorzugt; diese Klinik hat ca. 30 Ärzte. | 2026-05-13 |
| ADR-030 | ShiftHeatmap14 als eigenständiges Primitiv in components/dp/ | Die 14-Tage-Heatmap wird zunächst ohne echte Schichtdaten eingebettet (leere Boxen). Das Primitiv nimmt ein generisches `shifts`-Array entgegen, das in M2 mit echten API-Daten befüllt wird. Das Interface ist bewusst minimal gehalten: kein Tooltip-Overlay, keine Legende – der Fokus liegt auf dem schnellen visuellen Eindruck. | 2026-05-13 |
| ADR-031 | `soft`-Chip-Variante mit Sand-Token für Qualifikationen | Qualifikations-Chips brauchen eine pastelle, nicht-ablenkende Variante, die sich von Status-Chips (ok=grün, muted=grau) unterscheidet. `sand` (`#F3ECD8`) liegt auf dem warm-papiernen Spektrum des Atelier-Designs. Das Token ist in tokens.ts, tailwind.config.ts und index.css registriert; kein Hex-Code in Komponenten. | 2026-05-13 |
| ADR-032 | Übrige Stammdaten-Seiten: CommandBar + gerundeter Tabellen-Container | Schichttypen, Stationen, Qualifikationen und Sonderregelungen haben wenige (5–30) Einträge – eine kompakte Tabelle ist die richtige Wahl. Gemeinsamer Wrapper (`rounded-2xl border border-line bg-card overflow-hidden`) schafft visuelle Konsistenz. CommandBar ersetzt ad-hoc Page-Header-Patterns. | 2026-05-13 |
| ADR-033 | Schicht-Zuweisung weich validiert (nur Datenkonsistenz, keine Semantik) | Medizinischer Dienstplan-Alltag erfordert legitime Ausnahmen: Notfall-Vertretungen trotz Urlaub, Doppelbuchungen als Absicherung, Einsatz ohne Zertifikatserfüllung. Ein hartes Validierungsmodell würde solche Ausnahmen blockieren. Stattdessen: Backend prüft nur Datenkonsistenz (Shift existiert, Doctor existiert und ist aktiv). Semantische Konflikte (INA-Verfügbarkeit, Qualifikation, Doppelbuchung) werden read-only durch die Konflikt-Engine (M2-005) zurückgegeben und im Frontend markiert, aber nie als Schreibfehler behandelt. | 2026-05-18 |
| ADR-034 | PATCH /api/shifts/{id} ohne plan_id im Pfad | Shift-IDs sind global eindeutig. `plan_id` im Pfad wäre redundant und erzwingt eine unnötige Join-Abfrage nur zur Validierung der Zugehörigkeit. Lese-Endpunkte bleiben nested unter `GET /api/plans/{id}/shifts` (Collection per Parent). Schreib-Endpunkte adressieren das Objekt direkt per ID. Pattern: read-collection per Parent, update-single per globaler ID. | 2026-05-18 |
| ADR-035 | Konflikt-Engine read-only, plan-intern, kein Caching | Konflikte werden pro Request aus den aktuellen Daten berechnet. Kein Materialisieren, kein Caching über Requests hinweg. Für eine lokale Single-User-SQLite-App ist der Rechenaufwand akzeptabel. Caching würde Invalidierungslogik erfordern, die keinen Mehrwert bietet. | 2026-05-18 |
| ADR-036 | Nur zwei Konflikt-Typen: NOT_AVAILABLE und DOUBLE_BOOKED | Qualifikations-Konflikt ist vertagt — es gibt keine ShiftType→Qualifikation-Beziehung im Datenmodell. Tarif-/Ruhezeiten-/Wochenstunden-Constraints kommen mit dem Solver. Mit den aktuellen Daten sind nur diese zwei Typen zuverlässig berechenbar. | 2026-05-18 |
| ADR-037 | Offene Schichten sind kein Konflikt-Typ, sondern separates Aggregat | Ein halbfertiger Plan mit unbesetzten Schichten ist ein normaler Planungszwischenstand, keine Fehlermarkierung. Offene Schichten in `open_shifts`/`open_shift_count` zu trennen verhindert, dass die Konflikt-KPI durch planbare Leerstellen aufgebläht wird. | 2026-05-18 |
| ADR-038 | PATCH-Response trägt keine conflicts; Frontend refetcht | Ein Einzel-PATCH müsste für DOUBLE_BOOKED-Erkennung alle anderen Shifts desselben Arztes/Tags laden — unnötige Kopplung. In einer lokalen App ist ein separater GET /conflicts nach jeder Zuweisung billig. Die PATCH-Response bleibt schlank und entkoppelt. | 2026-05-18 |
| ADR-039 | DOUBLE_BOOKED markiert alle beteiligten Shifts der Gruppe | Nur die "zweite" Shift zu markieren würde dem Frontend verschweigen, welche Zellen hervorgehoben werden müssen. Alle Shifts einer (doctor_id, date)-Gruppe mit Größe > 1 werden markiert, damit das Grid vollständig reagieren kann. | 2026-05-18 |
| ADR-040 | PlanGrid — Zeilen sind Ärzte, Spalten sind Tage | Zeilen = aktive Ärzte (aus GET /api/doctors). Spalten = Kalendertage des Plans. Zellen zeigen den Schichttyp-Code der zugewiesenen Shifts dieses Arztes an diesem Tag. Unbesetzte Shifts tauchen in keiner Arzt-Zeile auf; sie sind über `open_shift_count` (KpiBar) und das DoctorAssignPopover zugänglich. Begründung: Planungskoordinatoren denken arzt-zentriert. | 2026-05-18 |
| ADR-041 | ContextPanel öffnet per Warn-Dot, DoctorAssignPopover per Zell-Klick | Warn-Dot (11×11 px, oben rechts) öffnet ContextPanel mit Konflikt-Details. Klick auf den Rest der Zelle öffnet DoctorAssignPopover für Zuweisungsänderungen. ShiftCell.onConflictDotClick stoppt Propagation damit beide Handler unabhängig sind. | 2026-05-18 |
| ADR-042 | Nur Monatsansicht in M2-003 | 14-Tage- und 1-Tages-Views wurden bewusst zurückgestellt. Pläne decken einen Kalendermonat ab; die Monatsansicht ist die primäre Nutzeranforderung. Views können in einem späteren Meilenstein additiv ergänzt werden. | 2026-05-18 |
| ADR-043 | useAssignShift ohne optimistic update | PATCH-Response enthält bewusst keine Konflikte (ADR aus M2-005). Invalidierung beider Queries (shifts + conflicts) nach onSuccess ist ausreichend für eine lokale SQLite-App. | 2026-05-18 |
| ADR-044 | Roadmap-Lücke M3–M7: M8 folgt direkt auf M2 | Die Milestones M3–M7 sind in ADR-006 und data-model.md referenziert (Rotationsmanagement, Verfügbarkeitskalender, Tarif-Vollintegration, Excel-Export, etc.), aber noch nicht ausgearbeitet. Der erste Solver-Schritt (M8-001) ist technisch unabhängig von M3–M7 und wird vorgezogen. Die Milestones werden nachgepflegt wenn die Phase-A-Basis stabil ist. **Status: Aufgelöst durch ADR-052 (2026-05-20).** | 2026-05-19 |
| ADR-045 | M8-001: `/solve` liefert nur Vorschlags-Diff (kein DB-Write) | Phase-A-Zuweisungen (manuell, gepinnt) bleiben vollständig erhalten. Der Solver-Endpunkt `POST /api/plans/{id}/solve` gibt `proposed_assignments` zurück, schreibt aber nie in die Datenbank. Das Anwenden des Vorschlags ist ein expliziter Folge-Schritt (Folge-Milestone). Maximale Phase-A-Unversehrtheit; keine versehentlichen Überschreibungen manueller Planung. | 2026-05-19 |
| ADR-046 | DOUBLE_BOOKED als erste Solver-Constraint | Kein Arzt darf am selben Kalendertag mehr als einmal eingeplant sein. Dies ist die einzige logisch-harte Constraint, die keine Tarif-Werte erfordert und empirisch vollständig verifizierbar ist. Alle regulatorischen Constraints (Ruhezeiten, Wochenstunden, ArbZG) kommen in Folge-Milestones. | 2026-05-19 |
| ADR-047 | Synchroner Solve-Aufruf (kein async/Queue) | Single-User-Lokal-App; asynchrone Verarbeitung (WebSocket, Job-Queue, SSE) würde erhebliche Infrastruktur erfordern ohne Mehrwert. 30s Termination-Limit ist für einen lokalen HTTP-Request akzeptabel. Kein Timeout auf FastAPI-Seite für diesen Endpunkt. | 2026-05-19 |
| ADR-048 | Termination-Limit 30 Sekunden (TERMINATION_SECONDS) | Kompromiss zwischen Lösungsqualität und Wartezeit. Für einen Monatsplan mit ~30 Ärzten und ~60 Schichten ist 30s ein großzügiges Budget; typische Lösungen werden in Sekunden gefunden. Die Konstante `TERMINATION_SECONDS` in `solver_service.py` ist zentral justierbar. | 2026-05-19 |
| ADR-049 | timefold-solver benötigt Java 17+ JVM (Laufzeit-Voraussetzung) | timefold-solver-python ist ein JPype-Bridge über die Java-Implementierung. Ohne Java 17+ startet die JVM nicht. Phase-A des Dienstplaners läuft ohne JVM (lazy imports). Java 17+ ist Pflicht für Phase B (Solver-Endpunkt). Empfohlen: Eclipse Temurin 21 (LTS). | 2026-05-19 |
| ADR-050 | Lazy JVM-Imports in solver_service.py (Phase-A-Invariante) | Alle timefold-Importe in `solver_service.py` sind lazy (innerhalb von `solve_plan()`). `solver/__init__.py` schützt JVM-abhängige Re-Exporte mit try/except. Damit startet die FastAPI-App auch ohne installierten JVM; der `/solve`-Endpunkt liefert 503 wenn JVM fehlt. | 2026-05-19 |
| ADR-051 | M8-002: `/apply` schreibt Solver-Vorschlag ohne JVM und ohne Pin-Änderung | `POST /api/plans/{id}/apply` nimmt `proposed_assignments` im Request-Body (Diff aus `/solve`), schreibt `doctor_id` in die DB — kein timefold-Import, kein Re-Solve. Gepinnte Shifts werden übersprungen (`skipped_pinned`), nicht überschrieben. `is_pinned` wird bewusst nicht gesetzt: Solver-Apply ist kein manueller Eingriff. Konflikte werden nicht in der Apply-Response berechnet; Client invalidiert und refetcht `shifts`+`conflicts` (Decoupling wie ADR-038). Alle Shifts in einer Transaktion, 422 bei Datenkonsistenz-Fehler (Plan/Shift/Doctor). | 2026-05-19 |
| ADR-052 | M3–M7-Cut: Phase-A-Abschluss vor weiterem Solver-Ausbau | Schließt ADR-044. Sequenz: **M3-001** Plan-Editor v2 mit Drag & Drop (dnd-kit) → **M4-001** Verfügbarkeit & Rotation Management UI (RotationAssignment, INAExclusion, EmploymentPeriod, Absence) → **M5-001** Tarif-Soft-Validierung als Framework (keine konkreten Tarif-Werte ohne Domänenklärung) → **M6-001** Excel-Export via openpyxl → **M7-001** Phase-A-Abschluss & Polish. Phase-B-Frontend (Solver Review-&-Apply UI) wird als separater Strang **M9-001** geführt, damit M3–M7 thematisch Phase-A-Abschluss bleibt und nicht von Solver-UX-Entscheidungen blockiert wird. Jeder Milestone deckt genau eine Kernfunktion ab (enger Cut). Vollständige Roadmap in `docs/roadmap.md`. | 2026-05-20 |
| ADR-053 | M3-001: DnD nur in Bereiche-Ansicht; Dienste-Ansicht bleibt beim Klick-Popover | Domain-Begründung: Rotation bestimmt die INA-Verfügbarkeit für Dienste — der natürliche DnD-Einstieg ist daher die Rotations-Zuweisung. Dienste-Ansicht (Schicht-Zuweisung) hat einen vollständig funktionalen Klick-Popover (DoctorAssignPopover), der nicht verdrängt werden soll. Dienste-DnD wird als separater Folge-Milestone geführt. | 2026-05-20 |
| ADR-054 | M3-001: Drop öffnet RotationAssignPopover mit preselectedDoctorId statt direktem DB-Write | RotationAssignment hat valid_from/valid_to (Zeitraum); ein 1-Tages-Default wäre fachlich falsch. Der bestehende RotationAssignPopover-Pfad erfordert Benutzerbestätigung des Zeitraums. Lösung: Drop setzt `preselectedDoctorId`-Prop am Popover — der Arzt ist vorausgewählt, der User bestätigt Zeitraum. Kein neuer Mutations-Pfad, keine neue Validierungslogik. Klick-Pfad (ohne Vorauswahl) bleibt als a11y-Fallback unverändert. | 2026-05-20 |
| ADR-055 | M3-001: Tab-Style Underline statt Pill für aktive Reiter in PlanPage | Pill-Style (`bg-accent text-white` = Terracotta-Hintergrund) wirkt neben terracottafarbenen Doctor-Avataren visuell überladen. Underline (`border-b-2 border-accent text-ink`) ist zurückhaltender und passt zum Atelier-Look. Nutzt ausschließlich bestehende dp-Tokens, kein neuer Token. | 2026-05-20 |
| ADR-056 | M4-001: Absence-CRUD in Arzt-Detail (Tab auf DoctorDetailPage), nicht als eigene Stammdaten-Seite | Konsistenz mit INAExclusion-Pattern (ADR-018): Abwesenheiten sind arzt-zentrierte, zeitabhängige Einträge, die kontextuell zur Arzt-Detailseite gehören. Kein separater Navigations-Eintrag im MiniRail. Code-Wiederverwendung durch 1:1-Übernahme der INAExclusion-Frontend-Struktur (List + FormDialog + Hook). Konsequenz: Kein separater Menüpunkt für Abwesenheiten — Zugriff ausschließlich über Arzt-Detail. | 2026-05-21 |
| ADR-057 | M4-001: `GET /api/doctors/{id}/ina-availability` als dünner Wrapper ohne Logik-Duplikation | ADR-016 mandatiert `get_ina_availability` als zentrale Funktion für INA-Verfügbarkeit. Der neue Endpoint ruft ausschließlich `get_ina_availability_for_period` aus `ina_availability_service.py` auf — keine eigene Drei-Quellen-Logik. Jede Änderung an der Verfügbarkeitslogik wirkt automatisch auf alle Consumer (Plan-Editor, Solver, API). `grep -n "blocks_ina"` in neuen API-Dateien muss 0 Treffer liefern. | 2026-05-21 |
| ADR-058 | M4-001: `<input type="date">` als Standard für Datumseingaben in Formularen | YAGNI-Entscheidung für lokale Single-User-Desktop-App: Browser-Native-Date-Input reicht vollständig aus. Kein zusätzliches Paket (shadcn Calendar, react-day-picker) eingeführt. Browser zeigt Datum lokal (DE-Format), sendet ISO — keine Konvertierung nötig. Konsistentes Pattern in AbsenceFormDialog, INAExclusionFormDialog und EmploymentPeriodForm. Konsequenz: Kein Kalender-Popup, kein Keyboard-Navigation-Enhancement — akzeptabel für Zielgruppe (Planungskoordinator, Desktop-Browser). | 2026-05-21 |
